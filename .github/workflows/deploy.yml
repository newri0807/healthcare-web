name: Deploy React
on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # <- active 버튼 생성

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: SSH Remote Commands
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_NAME }}
        key: ${{ secrets.SERVER_ACCESS_KEY }}
        script: |
          echo "🚀 React (Vite) '클린' 배포 시작..."
          
          # 1. 기존 폴더가 있으면 과감하게 삭제
          if [ -d "~/deploy/repo-healthcare" ]; then
            sudo rm -rf ~/deploy/repo-healthcare
          fi
          
          # 2. 깨끗한 상태에서 새로 다운로드
          cd ~/deploy
          git clone https://github.com/newri0807/healthcare-web.git repo-healthcare
          
          # 3. 실행 위치 이동
          cd ~/deploy
          
          # 4. 도커 실행
          docker compose up -d --build healthcare-web
          
          echo "✅ React 배포 완료!"
