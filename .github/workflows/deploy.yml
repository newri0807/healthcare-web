name: Deploy React
on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: SSH Remote Commands
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.KEY }}
        script: |
          echo "🚀 React (Vite) 배포 시작..."
          
          # 1. 최신 코드 받기 (프로젝트 폴더)
          cd ~/deploy/repo-healthcare
          git pull origin main
          
          # 2. 실행 위치 이동 (중요!)
          cd ~/deploy
          
          # 3. 도커 재시작
          # 서비스 이름을 docker-compose.yml과 똑같이 'healthcare-web'으로!
          docker compose up -d --build healthcare-web
          
          echo "✅ React 배포 완료!"
